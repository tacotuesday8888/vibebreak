import type {InkColor} from '../types.js';

export const colors = {
	brand: 'magenta' as InkColor,
	brandAlt: 'cyan' as InkColor,
	accent: 'yellow' as InkColor,
	saved: 'green' as InkColor,
	failed: 'red' as InkColor,
} as const;
