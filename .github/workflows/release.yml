name: Release

# Publishes vibebreak-arcade to npm whenever a SemVer tag (vX.Y.Z) is pushed.
#
# One-time setup before the first release:
#   1. Create an automation-type access token at https://www.npmjs.com/settings/<your-user>/tokens
#   2. Add it as a repository secret named `NPM_TOKEN`
#      (Settings -> Secrets and variables -> Actions -> New repository secret).
#   3. Make sure `package.json` `version` matches the tag you push.
#      `npm version patch|minor|major` updates package.json AND creates the tag.
#
# Cutting a release:
#   npm version patch
#   git push --follow-tags
#
# The workflow then runs typecheck, builds, and publishes with provenance.

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Verify tag matches package version
        run: |
          PKG_VERSION="$(node -p "require('./package.json').version")"
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "Tag $GITHUB_REF_NAME does not match package.json version $PKG_VERSION" >&2
            exit 1
          fi

      - name: Publish to npm
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
