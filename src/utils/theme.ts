import type {InkColor} from '../types.js';

export const colors = {
	brand: '#E633FF' as InkColor,
	brandAlt: '#37E7FF' as InkColor,
	accent: '#FFD84D' as InkColor,
	saved: '#27FF7A' as InkColor,
	failed: '#FF3E6C' as InkColor,
	blue: '#4E6BFF' as InkColor,
	purple: '#6F42FF' as InkColor,
	text: '#F8FAFC' as InkColor,
} as const;
